{% extends ":admin:base.html.twig" %}

{% block body %}
    <body {% block body_class %}{% endblock %}>
    <div class="container d-table">
        <div class="d-100vh-va-middle">
            <div class="row">
                {% block content %}
                    <h4>{{ 'resetting.password_already_requested'|trans({}, 'FOSUserBundle') }}</h4>
                {% endblock %}
            </div>
        </div>
    </div>
    </body>
{% endblock %}