
 <!-- El logotipo y el icono que despliega el menú se agrupan
       para mostrarlos mejor en los dispositivos móviles -->
  <div class="navbar-header">
    <button type="button" class="navbar-toggle" data-toggle="collapse"
            data-target=".navbar-ex1-collapse">
      <span class="sr-only">Desplegar navegación</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    <a class="navbar-brand" href="{{ path('dashboard') }}"><span class="glyphicon glyphicon-home"></span></a>
  </div>
 
  <!-- Agrupar los enlaces de navegación, los formularios y cualquier
       otro elemento que se pueda ocultar al minimizar la barra -->
  <div class="collapse navbar-collapse navbar-ex1-collapse">
    <ul class="nav navbar-nav">
      <li><a href="{{ path('reservation_list') }}">{{ 'index.reservations' | trans }}</a></li>  
      <li class="dropdown mega-dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">{{ 'admin.hotels' | trans }} {% if not app.session.get('hotel_id') %} <span class="glyphicon glyphicon-asterisk" style="color: red"></span> {% endif %}<span class="caret"></span></a>				
            <ul class="dropdown-menu mega-dropdown-menu">
                <li class="col-sm-3">
                    <ul>
                        <li class="dropdown-header">{{ 'admin.General_information' | trans }}</li>
                        <li><a href="{{ path('hotel_name') }}">{{ 'admin.name' | trans }} {% if not app.session.get('hotel_name') %} <span class="glyphicon glyphicon-asterisk" style="color: red"></span> {% endif %}</a></li>
                        <li><a href="{{ path('hotel_address') }}">{{ 'hotels.address' | trans }} {% if not app.session.get('hotel_address') %} <span class="glyphicon glyphicon-asterisk" style="color: red"></span> {% endif %}</a></li>
                        <li><a href="{{ path('hotel_stars') }}">{{ 'hotels.stars' | trans }} {% if not app.session.get('hotel_stars') %} <span class="glyphicon glyphicon-asterisk" style="color: red"></span> {% endif %}</a></li>
                        <li class="divider"></li>
                        <li class="dropdown-header">{{ 'admin.gallery_photo' | trans }}</li>
                        <li><a href="#">{{ 'hotels.pictures' | trans }} {% if not app.session.get('hotel_pictures') %} <span class="glyphicon glyphicon-asterisk" style="color: red"></span> {% endif %}</a></li>
                    </ul>
                </li>
                <li class="col-sm-3">
                    <ul>
                        <li class="dropdown-header">{{ 'admin.profile_trip.label' | trans }}</li>
                        <li><a href="#">{{ 'hotels.search_menu.type' | trans }} {% if not app.session.get('hotel_type') %} <span class="glyphicon glyphicon-asterisk" style="color: red"></span> {% endif %}</a></li>
                        <li><a href="#">{{ 'hotels.search_menu.hotel_chain' | trans }} {% if not app.session.get('hotel_chain') %} <span class="glyphicon glyphicon-asterisk" style="color: red"></span> {% endif %}</a></li>                            
                        <li><a href="#">{{ 'admin.profile_trip.label' | trans }} {% if not app.session.get('hotel_label') %} <span class="glyphicon glyphicon-asterisk" style="color: red"></span> {% endif %}</a></li>
                        <li><a href="#">{{ 'hotels.services' | trans }} {% if not app.session.get('hotel_services') %} <span class="glyphicon glyphicon-asterisk" style="color: red"></span> {% endif %}</a></li>
                    </ul>
                </li>
                <li class="col-sm-3">
                    <ul>
                        <li class="dropdown-header">{{ 'admin.Much_more' | trans }}</li>
                        <li><a href="#">{{ 'hotels.description' | trans }}</a></li>                      
                    </ul>
                </li>
            </ul>				
      </li>  
      <!--<li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown">
          {#{ 'admin.hotels' | trans }} <b class="caret"></b>
        </a>
        <ul class="dropdown-menu">
          <li><a href="{{ path('hotel_list') }}">{{ 'admin.hotels_list' | trans }}</a></li>
          <li><a href="{{ path('hotel_new') }}">{{ 'admin.add' | trans }#}</a></li>
        </ul>
      </li>-->
      <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown">
          {{ 'index.search.hotel.rooms' | trans }} <b class="caret"></b>
        </a>
        <ul class="dropdown-menu">
          <li><a href="{{ path('room_list') }}">{{ 'admin.promotion_section_list' | trans }}</a></li>
          <li><a href="{{ path('room_new') }}">{{ 'admin.add' | trans }}</a></li>
        </ul>
      </li>
      <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown">
          {{ 'hotels.beds' | trans }} <b class="caret"></b>
        </a>
        <ul class="dropdown-menu">
          <li><a href="{{ path('bed_list') }}">{{ 'admin.promotion_section_list' | trans }}</a></li>
          <li><a href="{{ path('bed_new') }}">{{ 'admin.add' | trans }}</a></li>
        </ul>
      </li>
    </ul>
        
    <ul class="nav navbar-nav navbar-right">
      {% if is_granted('ROLE_ADMIN') %}  
      <li><a href="#">Enlace #3</a></li>
      <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown">
          {{ 'admin.promotions' | trans }} <b class="caret"></b>
        </a>
        <ul class="dropdown-menu">
          <li><a href="#">{{ 'admin.promotion_section' | trans }}</a></li>
          <li><a href="{{ path('promotion_sections_list') }}">{{ 'admin.promotion_section_list' | trans }}</a></li>
          <li><a href="{{ path('promotion_sections_new') }}">{{ 'admin.add' | trans }}</a></li>
          <li><a href="{{ path('promotion_list') }}">{{ 'admin.promotions_list' | trans }}</a></li>
          <li><a href="{{ path('promotion_new') }}">{{ 'admin.promotion_add' | trans }}</a></li>
        </ul>
      </li>
      {% endif %}
      <form class="navbar-form navbar-left" role="search">
        <div class="form-group">
          <input type="text" class="form-control" placeholder="Buscar">
        </div>
        <button type="submit" class="btn btn-default">Enviar</button>
      </form>
    </ul>
  </div>