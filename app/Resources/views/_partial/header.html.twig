<div class="container-fluid">
    <button class="navbar-toggler mobile-toggler hidden-lg-up" type="button">☰</button>
    <div class="col-md-11">
        <a class="navbar-brand" href="{{ path('dashboard') }}">
            <img style="display: block; width: 130px;" src="{{ asset('bundles/vientosurappapp/images/vientosur-logo-color.png') }}" alt="">
        </a>
        <div class="titletext">Extranet</div>
    </div> 
    {% if app.user %}  
    <ul class="nav navbar-nav navbar-right" style="margin-right: 25px;">
        <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                <span class="glyphicon glyphicon-user"></span> 
                <strong>{{ app.user.firstName }} {{ app.user.lastName }}</strong>
                <span class="glyphicon glyphicon-chevron-down"></span>
            </a>
            <ul class="dropdown-menu" style="width: 280px; padding: 5px;">
                <li>
                    <div class="navbar-login">
                        <div class="row">
                            <div class="col-lg-4">
                                <p class="text-center">
                                    <span class="glyphicon glyphicon-user icon-size" style="font-size: 93px; padding: 20px 20px 10px 5px;"></span>
                                </p>
                            </div>
                            <div class="col-lg-8">
                                <p class="text-left"><strong>{{ app.user.firstName }} {{ app.user.lastName }}</strong></p>
                                <p class="text-left small">{{ app.user.email }}</p>
                                <p class="text-left">
                                    <a href="{{ path('fos_user_profile_show') }}" class="btn btn-primary btn-block btn-sm">Actualizar Datos</a>
                                </p>
                            </div>
                        </div>
                    </div>
                </li>
                <li class="divider"></li>
                <li>
                    <div class="navbar-login navbar-login-session">
                        <div class="row">
                            <div class="col-lg-12">
                                <p>
                                    <a href="{{ path('fos_user_security_logout') }}" class="btn btn-danger btn-block">Cerrar Sesion</a>
                                </p>
                            </div>
                        </div>
                    </div>
                </li>
            </ul>
        </li>
    </ul>    
    {% endif %}        
</div>