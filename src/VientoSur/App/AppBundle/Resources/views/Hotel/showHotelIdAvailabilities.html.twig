Name: {{hotel[0].name}} /
Estrallas: {{hotel[0].stars}}
<br>
<div>
<img src="{{hotel[0].pictures[0].url}}" width="400"  height="300">
</div>
<br>

Información: {{hotel[0].information.es}}
<br>    
{#    Key "items" for array with keys "choice, payment_type, meal_plan, rooms, 
cancellation_policy, promo, price_detail, contract_currency, payment_methods" #}
<br>
Nuemero de habitaciones: {{item.roompacks[0].rooms|length}}
<br>
{%for room in item.roompacks[0].rooms%}
    {#{{room.kkl}}#}
    Referencia:{{ room.reference}}
    Nombre :{{ room.name}}
    Capacidad: {{ room.capacity.max}}
    Codigo: {{ room.room_code}}
    Occupation Personas: 
    {%for main_guest in  room.occupation.main_guests%}
         {{main_guest.guest_age_type_id}}/Total: {{main_guest.quantity}}
    {%endfor%}   
    <br>
    {#"currency, subtotal, taxes, taxes_detail, fee, discounts, total, nightly"#}
{%endfor%}
Servicios: <bR>
{%for amenitie in hotel[0].amenities %}
    {{ amenitie.descriptions.es}}<br>
{%endfor%}
Precio del paquete: <br>
Moneda:{{item.roompacks[0].price_detail.currency}}
subtotal:{{item.roompacks[0].price_detail.subtotal}}
taxes:{{item.roompacks[0].price_detail.taxes}}
taxes_detail: {{item.roompacks[0].price_detail.taxes_detail|length}}
fee: {{item.roompacks[0].price_detail.fee}}
discounts: {{item.roompacks[0].price_detail.discounts}}
total: {{item.roompacks[0].price_detail.total}}
nightly: {{item.roompacks[0].price_detail.total}}
Metodos de pago del Paquete: {{item.roompacks[0].payment_methods|length}}