<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Viento Sur - Reservaci&oacute;n</title>
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" rel="stylesheet" type="text/css" />
    <style type="text/css">
        .header-top {
            max-height: 100%;
            padding: 5px 0;
            background: #404040;
        }
        .logo {
            display: block;
        }
        a {
            color: #337ab7;
            text-decoration: none;
        }
        header#main-header {
            border-bottom: 2px solid #ed8323;
        }
        .header-top {
            max-height: 100%;
            padding: 5px 0;
            background: #404040;
        }
    </style>
</head>
<body>
<header id="main-header">
    <div class="header-top">
        <div class="container">
            <div class="col-md-3">
                <a class="logo" href="#">
                    <img src="http://www.vientosur.net/bundles/vientosurappapp/images/vientosur-logo-color.png" alt="Viento Sur" title="">
                </a>
            </div>
        </div>
    </div>
</header>

<div class="container bs-docs-container">
    <table class="table">
        <tr>
            <th colspan="2">
                <b>
                    <h3>Su solicitud de cancelación ha sido procesada.</h3>
                </b>
            </th>
        </tr>
        <tr>
            <th colspan="2">
                <table class="table">
                    <tr>
                        <td>
                            <img src="{{ hotelDetails.main_picture.url }}/200x150" />
                        </td>
                        <td>
                            <h3>{{ hotelDetails.name }}</h3>
                        </td>
                    </tr>
                </table>
            </th>
        </tr>
        <tr>
            <th width="15%">
                Direcci&oacute;n:
            </th>
            <td>
                {{ hotelDetails.location.address }}.
            </td>
        </tr>
    </table>

    <table class="table table-bordered">
        <tbody>
        <caption>Hemos cancelado una reservación con las siguientes características: </caption>
        <tr>
            <th width="20%">
                Tu reserva:
            </th>
            <td>
                {{ reservationDetails.night_count }} noche(s). {{ reservationDetails.rooms|length }} Habitacion(es).
            </td>
        </tr>
        <tr>
            <th>
                Entrada:
            </th>
            <td>
                {{ reservationDetails.checkin_date|date("d/m/Y") }}
            </td>
        </tr>
        <tr>
            <th>
                Salida:
            </th>
            <td>
                {{ reservationDetails.checkout_date|date("d/m/Y") }}
            </td>
        </tr>
        <tr>
            <th>
                N&uacute;mero de reserva:
            </th>
            <td>
                {{ reservationDetails.id }}
            </td>
        </tr>
        <tr>
            <th>
                Habitaci&oacute;n(es):
            </th>
            <td>
                <ul>
                    {% for room in reservationDetails.rooms %}
                        <li>{% if room.descriptions.es is defined %}{{ room.descriptions.es }}, {% endif %}{{ room.adults }} Adulto(s) {% if room.children > 0 %}{{ room.children }} Niño(s){% endif %}</li>
                    {% endfor %}
                </ul>
            </td>
        </tr>
        <tr>
            <th>
                Número de cancelación
            </th>
            <td>
                <li>{{ idCancellation }}</li>
            </td>
        </tr>
        </tbody>
    </table>
</div>

    <script src="http://code.jquery.com/jquery-1.10.2.min.js"></script>
    <script src="http://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
</body>
</html>
