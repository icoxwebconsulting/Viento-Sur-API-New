{% for message in app.session.flashBag.get('danger') %}
    <div class="alert alert-danger no-border">
        <button type="button" class="close" data-dismiss="alert"><span>×</span><span class="sr-only">x</span></button>
        <span class="text-semibold">{{ 'index.error' | trans }}!!</span> {{ message }}
    </div>
{% endfor %}